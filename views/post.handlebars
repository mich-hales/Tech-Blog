<div class="card m-3 p-3">
  <h5 class="post-title">{{post.title}}</h5>

  <div class="card-body">
    <p class="card-title">Posted by: {{post.user.name}} on {{post.date}}</p>
    <p class="card-text">{{post.content}}Insert the post content here</p>
  </div>
</div>

<div class="row justify-content-center">
  <h3>Comments:</h3>
</div>

{{#each comm as |com|}}
<div class="card m-3">
  <div class="card-body">
    <p class="card-text">{{com.content}}</p>
    <p class="card-title">Posted by: {{com.user.name}} on {{com.date}} -- edith with proper handlebars syntax</p>
  </div>
</div>

<div class="row justify-content-center">
  <h3>Leave a Comment:</h3>
</div>

<form class="m-3">
  <div class="form-group">
    <textarea rows="3" class="form-control" placeholder="Leave a Comment" id="comment-text"></textarea>
    <div class="row justify-content-center">
      <button class="btn btn-primary m-2" type="submit" id="btn">Submit</button>
    </div>
    
  </div>
</form>
<script src="/js/comment.js"></script>